machine:
  services:
    - docker
dependencies:
  override:
    - docker build --rm=false --tag jasonkuhrt/continuous:$CIRCLE_SHA1 .
test:
  override:
    - docker run jasonkuhrt/continuous:$CIRCLE_SHA1 npm run test
deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push jasonkuhrt/continuous:$CIRCLE_SHA1
